# Frozen Version 1.0 submission

The immutable submission is `releases/1.0/Assignment1-submission.zip`. Review `report.pdf` and `site/index.html` (served over HTTP). The active source tree is now organized for Version 2.0 development and is not a replacement 1.0 release.

Release notes: `releases/1.0/RELEASE-1.0.md`. Validate the frozen ZIP with `releases/1.0/Assignment1-submission.zip.sha256`; its `MANIFEST-1.0.json` records individual payload hashes.

The report follows the assignment's five Basic Tasks (A1–A5) and three Advanced Tasks (B1–B3), followed by OpenCode decisions and reproduction details (C).

`backup/` contains historical versions and is excluded from the current archive. The active source packages are `src/`, `pipelines/`, `reporting/`, `checks/` and `tools/`; locked `data/` and `site/` remain at the project root. Public HF publication requires local authentication.
